# Shell scripts run on Linux (VPS installers bundled into the apps): always LF, even in a Windows
# checkout with core.autocrlf — a CRLF deploy.sh dies on the server with a syntax error.
*.sh text eol=lf
