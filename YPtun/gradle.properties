#Gradle
org.gradle.jvmargs=-Xmx6G
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.daemon=true
org.gradle.parallel=true
# Use all 12 cores for parallel task execution (was defaulting to a single worker in practice).
org.gradle.workers.max=12
# Compile independent modules' Kotlin in parallel within the same project.
kotlin.parallel.tasks.in.project=true

#Kotlin
kotlin.daemon.jvmargs=-Xmx4G
kotlin.native.binary.gc=cms
kotlin.incremental.wasm=true
org.jetbrains.compose.experimental.macos.enabled=true
compose.desktop.packaging.checkJdkVendor=false
org.gradle.java.installations.auto-detect=true
org.gradle.java.installations.auto-download=false
olcbox.version=3.3.2
olcbox.versionCode=332
# Идентичность приложения для Android. Переезд состоялся в 3.3.2: мост (LegacyExportProvider) уехал
# в 3.3.1, поэтому сборка с новым id ставится РЯДОМ со старой и при первом запуске забирает её данные
# (LegacyMigration.runIfNeeded), после чего один раз предлагает удалить старую установку.
# Больше НЕ МЕНЯТЬ: id — единственное, по чему обновление встаёт поверх установленного.
olcbox.applicationId=org.yptun.app

#Android
android.useAndroidX=true
android.r8.failOnMissingKotlinMetadata=false
kotlin.metadata.ignore.unsupported=true

# AGP 9.0+ performance and cleanup
android.generateSyncIssueWhenLibraryConstraintsAreEnabled=false
